services:
  - type: web
    name: clinforge
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -e .
    startCommand: uvicorn cdiscbuilderv2.app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
